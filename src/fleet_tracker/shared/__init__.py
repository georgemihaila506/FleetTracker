"""Shared foundation used by every component.

    config.py         runtime settings loaded from env (pydantic-settings)
    redis_client.py   async Redis connection factory      (added next)
    models.py         wire message models (pydantic)       (added next)
"""
